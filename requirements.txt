twisted==12.0.0
autobahn==0.5.14
websocket-client==0.12.0
sh==1.08
pyserial>=2.6
# Made Pillow optional (to minimize installation headaches).
# User is now prompted to install Pillow once it is reqired.
# Pillow>=2.2.1
# Only if using bluetooth connection to Pebble:
# git+http://github.com/pebble/lightblue-0.4.git#egg=lightblue-0.4
